# Railway configuration for SvaPro
[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "bash /app/start.sh"
restartPolicyType = "always"
restartPolicyMaxRetries = 10

[variables]
PORT = "8000"
